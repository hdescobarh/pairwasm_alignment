# Commands for

Docs

```shell
cargo rustdoc -- --default-theme=dark --document-private-items
```

Web release

```shell
wasm-pack build --target web --out-dir dist
```

npm release

```shell
wasm-pack build
wasm-pack publish
```
